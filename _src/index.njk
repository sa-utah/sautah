---
layout: base
metaTitle: Sexaholics Anonymous in Utah
bodyClass: home
---
{# Use new upcomingNews collection from Eleventy config #}
{% set upcoming_news = collections.upcomingNews %}

<div class="hero jumbotron parallax text-white text-center">
  <div class="container d-flex h-100 flex-column justify-content-center align-items-center">
    <h1 class="text-uppercase display-4 font-weight-bold">SA Utah</h1>
    <p class="h5 font-weight-bold">If you want the solution that we’ve found, we’d love to share it with you. Join us at a meeting near you.</p>
    <p class="my-3">
      <a href="{{ '/meetings' | url }}" class="btn btn-primary">Find a Meeting</a>
    </p>
  </div>
</div>
<div class="container bg-white shadow-sm p-4 p-sm-5">
  <div class="row mb-4">
    <div class="col-12">
      <div class="alert alert-info text-center" style="font-size:1.25rem;">
        <strong>Vision:</strong> To provide a safe, supportive, and anonymous environment for recovery from sexaholism in Utah. Learn more on our <a href="{{ '/about-sa' | url }}">About SA</a> and <a href="{{ '/for-newcomers' | url }}">For Newcomers</a> pages.
      </div>
    </div>
  </div>
  <h3>Events</h3>
  <br>
  <div class="row">


    {%- if upcoming_news.length > 0 -%}
      {%- for post in upcoming_news -%}
      <div class="col-12 col-md-6">
        <div class="card mb-4 mb-sm-0">
          <img src="{{ post.data.image | url }}" class="card-img-top">
          <div class="card-body">
            <h2 class="card-title h5">{{ post.data.title }}</h2>
            <p class="card-text">{{ post.data.excerpt }}</p>
            <p class="card-text"><strong>Date:</strong> {{ post.data.startDate }}{% if post.data.endDate %} - {{ post.data.endDate }}{% endif %}</p>
            <a href="{{ post.url | url }}" class="btn btn-outline-primary">Read More</a>
          </div>
        </div>
      </div>
      {%- endfor -%}
    {%- else -%}
      <div class="col-12">
        <div class="alert alert-info text-center">No upcoming news or events at this time.</div>
      </div>
    {%- endif -%}
  </div>
  <div class="row mt-4">
    <div class="col-12 text-center">
      <a href="{{ '/past-events' | url }}" class="btn btn-secondary">View Past Events</a>
    </div>
  </div>
</div>