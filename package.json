{
  "name": "sautah",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf _site && npx @11ty/eleventy --serve --quiet --port=3000",
    "forestry:preview": "eleventy --serve --port 8080 --output=dist",
    "debug": "rm -rf _site && DEBUG=Eleventy* npx @11ty/eleventy --serve --port=3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "1.0.2"
  }
}
